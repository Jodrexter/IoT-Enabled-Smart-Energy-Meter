<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartMeter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/back_dark</item>
        <item name="colorAccent">@color/golden</item>
    </style>

    <style name="ActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/back_dark</item>
        <item name="colorAccent">@color/golden</item>

    </style>

    <style name="Theme.SmartMeter" parent="Base.Theme.SmartMeter" />
</resources>